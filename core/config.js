;(function initConfig(){
  try{ window.PG = window.PG || {}; }catch(_){ return; }
  const config = {
    center: { x: 430, y: 430 },
    guideRadius: 330,
    coreMargin: 70
  };
  window.PG.config = config;
})();


